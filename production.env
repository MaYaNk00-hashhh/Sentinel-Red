# Production Environment Variables for Sentinel Red
# Upload the content of this file to Vercel Environment Variables

# Backend Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (Supabase)
# Copy these from your project dashboard or local .env
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Authentication
# Used for signing JWT tokens. You can generate a random string or use the one from local .env
JWT_SECRET=

# AI Service Configuration
# Required for Attack Graph Analysis and Recommendations
# Supports Google Gemini (starts with AIza) or AWS Bedrock (starts with ABSK)
LLM_API_KEY=

# Frontend Configuration
# Points the frontend to the backend API (relative path for Vercel)
VITE_API_BASE_URL=/api
